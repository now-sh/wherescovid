{
  "name": "location-covid-tracker",
  "version": "2.0.0",
  "description": "Location-based COVID-19 tracker with Dracula theme",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start": "parcel src/index.html",
    "build": "NODE_ENV=production parcel build src/index.html --no-source-maps --dist-dir public",
    "dev": "parcel src/index.html --dist-dir dist",
    "github": "NODE_ENV=production parcel build src/index.html --no-source-maps --dist-dir docs",
    "serve": "node server.js",
    "serve:build": "npm run build && npm run serve",
    "deploy": "gitcommit deploy && NODE_ENV=production parcel build src/index.html --no-source-maps --dist-dir public && vercel && vercel --prod",
    "lint": "eslint src/ --fix"
  },
  "keywords": ["covid19", "location", "tracker", "geolocation", "dracula-theme"],
  "author": "Jason M. Hempstead (Casjay) <git-admin@casjaysdev.com> (https://location.covid.casjay.coffee)",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "eslint": "^9.35.0",
    "globals": "^15.14.0",
    "@eslint/js": "^9.16.0",
    "sass": "^1.81.1",
    "@parcel/transformer-sass": "^2.12.0"
  }
}
